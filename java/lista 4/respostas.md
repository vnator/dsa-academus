# Respostas da Lista 4

3.a) Resposta: o erro da classe Y, esta no fato de que o atributo cont na class X eh declarado como privado, entao para alterar o valor do atributo no objeto ob, eh necessario usar o metodo setCont;


5) A capacidade de sobrecarga nao pode ser utilizada para metodos com mesmo nome e argumentos do mesmo tipo, independente do retorno, entao o codigo esta errado.

6) deve ser estatico pois dai se torna um atributo da classe e nao de uso especifico de um dos objetos instanciados.

ex:

```
class Carambolas {

  public static int numero = 10;
}
```

7) Private

8) pass-by-value
```
```
