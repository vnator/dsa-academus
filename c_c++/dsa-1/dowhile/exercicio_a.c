#include <stdio.h>

int main() {
    float a, b;

    printf("Entre com o Dividendo: ");
    scanf("%.2f", %a);

    do {
        printf("Entre com o Divisor: ");
        scanf("%.2f", %b);
    } while (b != 0);


    printf("%.2f / %.2f = %.2f", a, b, a/b);

    return 0;
}